const reducer = (state = 5) => {
    return state;
  }
  const store = Redux.createStore(reducer);

  //ı started learned redux

//----------------------------------

//const store = Redux.createStore(
//    (state = 5) => state
//  );
  
//  const currentState = store.getState();


//------------------------------------
let action = {
    type:'LOGIN'
  };


{/*  
  const action = {
    type: 'LOGIN'
  }
  
  function actionCreator(){
  return action;
  }
  */}
  