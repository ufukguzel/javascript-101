function booWho(bool){
    return typeof bool === "boolean";
}
booWho(null);

//bu fonksiyon false değeri döndürür çünkü girilen değer nulldur
//eğer "boolean" değeri gibi bir değer girilirse mesela "ufuk" true döndürüf
